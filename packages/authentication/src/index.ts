/**
 * @atlas/authentication — Tier 1
 * Supabase auth wrappers, session handling, and RBAC guards.
 *
 * Skeleton entry point. Real exports land in this package's build stage.
 * See docs/atlas/README.md.
 */
export const PACKAGE_NAME = '@atlas/authentication' as const
