/**
 * @atlas/cms — Tier 1
 * Typed content access layer over Supabase (queries, mutations, realtime).
 *
 * Skeleton entry point. Real exports land in this package's build stage.
 * See docs/atlas/README.md.
 */
export const PACKAGE_NAME = '@atlas/cms' as const
